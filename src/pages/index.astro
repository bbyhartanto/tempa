---
import Layout from "../layouts/home.astro";
import "../styles/global.css";
import "../styles/home.css";
import SectionServices from "../components/SectionServices.astro";
import SectionTechStack from "../components/SectionTechStack.astro";
import SectionCoreServices from "../components/SectionCoreServices.astro";

// Welcome to Astro! Wondering what to do next? Check out the Astro documentation at https://docs.astro.build
// Don't want to use any of this? Delete everything in this file, the `assets`, `components`, and `layouts` directories, and start fresh.
---

<Layout>
	<section class="relative min-h-screen flex flex-col">
		<!-- Background Grid -->
		<div
			class="absolute inset-0 grid grid-cols-4 md:grid-cols-8 gap-[1px] bg-gray-200 z-0 pointer-events-none"
		>
			<div class="bg-[#ffffff]"></div>
			<div class="bg-[#ffffff]"></div>
			<div class="bg-[#ffffff]"></div>
			<div class="bg-[#ffffff]"></div>
			<!-- md hidden -->
			<div class="hidden md:block bg-[#ffffff]"></div>
			<div class="hidden md:block bg-[#ffffff]"></div>
			<div class="hidden md:block bg-[#ffffff]"></div>
			<div class="hidden md:block bg-[#ffffff]"></div>
		</div>

		<!-- Foreground Content -->
		<div class="relative z-10 grid grid-cols-4 md:grid-cols-8 items-end">
			<div
				class="hidden md:block col-span-1 md:col-span-2 md:col-start-2 p-4 pointer-events-auto"
			>
				<p>
					Web studio specializing in UI/UX design and web development.
					Custom website and e-commerce optimized for conversion and
					performance.
				</p>
			</div>
			<div
				class="col-span-1 md:col-span-1 pointer-events-auto aspect-square overflow-hidden"
			>
				<img
					src="/home-img/hat.webp"
					alt="image hat online"
					class="w-full h-full object-cover"
				/>
			</div>
			<div></div>
			<div
				class="col-span-1 md:col-span-1 md:col-start-6 pointer-events-auto aspect-square overflow-hidden"
			>
				<img
					src="/home-img/hat.webp"
					alt="image hat online"
					class="w-full h-full object-cover"
				/>
			</div>

			<div
				class="col-span-1 md:col-span-1 pointer-events-auto aspect-square overflow-hidden"
			>
				<img
					src="/home-img/hat.webp"
					alt="image hat online"
					class="w-full h-full object-cover"
				/>
			</div>
		</div>
		<!-- row 2 -->
		<div class="relative z-10 grid grid-cols-4 md:grid-cols-8 items-end">
			<div
				class="col-span-1 md:col-span-3 md:col-start-2 p-4 pointer-events-auto @container"
			>
				<!-- The text size uses Container Query units (cqw) to scale relative to the parent div's width -->
				<!-- 25cqw means 25% of the container's width. You can adjust this number to fit perfectly -->
				<p>Introducing</p>
				<h3
					class="panchang-medium uppercase text-[22cqw] md:text-[22cqw] leading-none whitespace-nowrap relative z-20"
				>
					Studio
				</h3>
			</div>
			<div
				class="col-span-1 md:col-span-1 md:col-start-5 pointer-events-auto aspect-square overflow-hidden"
			>
				<img
					src="/home-img/hat.webp"
					alt="image hat online"
					class="w-full h-full object-cover"
				/>
			</div>
		</div>
		<!-- row 3 -->
		<div class="relative z-10 grid grid-cols-4 md:grid-cols-8 items-end">
			<div
				class="col-span-4 md:col-span-6 md:col-start-2 p-4 pointer-events-auto @container"
			>
				<!-- The text size uses Container Query units (cqw) to scale relative to the parent div's width -->
				<!-- 25cqw means 25% of the container's width. You can adjust this number to fit perfectly -->
				<h3
					class="panchang-medium uppercase text-[18cqw] leading-none whitespace-nowrap"
				>
					Tempa
				</h3>
			</div>
			<div
				class="col-span-1 col-start-2 md:col-span-1 md:col-start-5 pointer-events-auto aspect-square overflow-hidden"
			>
				<img
					src="/home-img/hat.webp"
					alt="image hat online"
					class="w-full h-full object-cover"
				/>
			</div>
			<div class="col-span-2 md:hidden p-4 pointer-events-auto">
				<p>
					Web studio specializing in UI/UX design and web development.
					Custom website and e-commerce optimized for conversion and
					performance.
				</p>
			</div>
		</div>
	</section>
	<SectionServices />
	<SectionCoreServices />
	<SectionTechStack />
	<!-- call to action -->
	<section class="min-h-[800px] flex flex-col justify-center">
		<div class="grid grid-cols-1 md:grid-cols-10 gap-2 py-10">
			<div
				class="col-start-1 col-span-1 md:col-start-2 md:col-span-8 pt-8 px-8"
			>
				<h2 class="text-h2">
					First impression matters. Make it count with a landing page
					that converts.
				</h2>
			</div>
		</div>
	</section>
	<!-- services section -->

	<script is:inline>
		// Trigger .reveal animation every time element enters viewport
		const revealObserver = new IntersectionObserver(
			(entries) => {
				entries.forEach((entry) => {
					if (entry.isIntersecting) {
						entry.target.classList.add("in-view");
					} else {
						entry.target.classList.remove("in-view"); // reset so animation replays on re-entry
					}
				});
			},
			{ threshold: 0.15 }
		);

		document.querySelectorAll(".reveal, .tablinks").forEach((el) => {
			revealObserver.observe(el);
		});
	</script>
</Layout>
